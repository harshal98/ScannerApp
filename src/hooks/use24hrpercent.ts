

function use24hrpercent() {

    // return axios.get(`https://api.binance.com/api/v3/ticker/24hr?symbols=[${FuturePairs.map(item=>`"${item}"`)}]`).then((resonse  : AxiosResponse) =>{
       
    // let data :{pair : string, priceChangePercent : number}[] =  resonse.data.map((item24tick : {symbol : string, priceChangePercent : string})=>{return {pair : item24tick.symbol, priceChangePercent : Number(item24tick.priceChangePercent)}

   
      
    //        })
    //        return data


           
              
 
// })


}
export default use24hrpercent;
